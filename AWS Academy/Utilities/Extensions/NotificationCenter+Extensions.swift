import Foundation

extension Notification.Name {
    /// Se publica cuando un usuario completa el registro (confirmó su email).
    static let userDidRegister = Notification.Name("userDidRegister")
}
